# 布局

## 全局布局

### nav-layout

nav-layout 包含默认导航和可选底部

|属性|类型|默认值|描述|
|---|---|---|---|
|title|string|""|副标题|
|set|number|2|选项个数|
|foo|boolean|false|禁用底部|

如果title为 `""` 则不显示副标题,否则将会是 `|` 与副标题联合的字符串

set将会向下取整显示设定的选项

foo如果为 `true` 则不显示底部

